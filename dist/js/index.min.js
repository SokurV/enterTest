"use strict";

var BTN_PLAY = document.getElementById("button-play"),
  POPUP_CONTAINER = document.getElementById("popup-container"),
  POPUP_VIDEO = document.getElementById("popup-video"),
  FIND_BTN = document.getElementById("find-out-button"),
  PRODUCTS_BLOCK = document.getElementById("products");
function popupOpenClose(t) {
  "button-play" === t.target.id && POPUP_CONTAINER.classList.toggle("display_none"), "button-close" !== t.target.id && "popup-container" !== t.target.id || POPUP_CONTAINER.classList.toggle("display_none");
}
function smoothScroll(t) {
  window.scrollTo({
    block: "center",
    top: t.offsetTop,
    behavior: "smooth"
  });
}
document.addEventListener("click", popupOpenClose);